import React from "react";
import ReactDOM from "react-dom/client";
import TriModelPredictor from "../mega-millions-fluid.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TriModelPredictor />
  </React.StrictMode>
);
