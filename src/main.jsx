import React from "react";
import ReactDOM from "react-dom/client";
import MegaMillionsFluid from "../mega-millions-fluid.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MegaMillionsFluid />
  </React.StrictMode>
);
